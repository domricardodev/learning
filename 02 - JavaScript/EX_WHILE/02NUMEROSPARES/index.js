/* 2. Números pares de 1 a 20

Enunciado:
Imprima os números pares de 1 a 20 utilizando while.
 */

let par = 2; // Começamos com o primeiro número par (2)

while (par <= 20) {
  console.log(par);
  par += 2; // Incrementa 'i' de 2 em 2 para pegar apenas os números pares
}