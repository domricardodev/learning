function celsiusParaFahrenheit(celsius) {
  // Fórmula de conversão para Fahrenheit.
  return (celsius * 9 / 5) + 32;
}

alert(celsiusParaFahrenheit(0)); // 32
alert(celsiusParaFahrenheit(100)); // 212