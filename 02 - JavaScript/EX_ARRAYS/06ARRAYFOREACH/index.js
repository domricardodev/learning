let animals = ["Dog", "Cat", "Elephant"];


// `forEach` executa a função para cada elemento do array
animals.forEach(function (animals) {
  console.log(animals);
});

// Uso do .forEach(), que é mais simples que for.